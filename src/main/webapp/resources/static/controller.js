/*
var mainController = angular.module('MenuController', ['$router', MenuController]);

mainController.controller('MenuController', ['$scope', '$http', '$log']
    function MenuController($router) {
        $router.config([
            { path: '/', redirectTo: '/galleries' },
            { path: '/galleries', component: 'galleries' },
            { path: '/gallery/:id', component: 'gallery' }
        ]);
});*/
