package by.bvpatent.web;import by.bvpatent.model.Account;import by.bvpatent.service.AccountService;import org.omg.CosNaming.Binding;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.http.HttpRequest;import org.springframework.stereotype.Controller;import org.springframework.validation.BindingResult;import org.springframework.web.bind.annotation.*;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import java.util.Collection;import java.util.Map;@RestControllerpublic class AccountController {    @Autowired    private AccountService accountService;    @RequestMapping("/")    public String homeAll1(HttpServletRequest request){        //System.out.println(request.getServletPath());        return "path - > " + request.getPathInfo();    }    @RequestMapping(value = "/account", method = RequestMethod.GET)    public Collection<Account> listContacts(){ //Map<String, Object> map) {        return accountService.getAllAccount();    }/*    @RequestMapping(value = "/add", method = RequestMethod.POST)    public @ResponseBody addAccount(@ModelAttribute("account")Account account, BindingResult result){        return accountService.addAccount(account);    }    @RequestMapping("/delete/{id}")    public String deleteAccount(@PathVariable("id") Integer accountId){        accountService.removeAccount(accountId);        return "redirect:/index";    }*/}