package by.bvpatent.web;import by.bvpatent.model.Account;import by.bvpatent.service.AccountService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.http.HttpHeaders;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;import org.springframework.web.util.UriComponentsBuilder;import java.util.Collection;@RestControllerpublic class AccountController {    @Autowired    private AccountService accountService;    @RequestMapping(value = "/accounts/all", method = RequestMethod.GET)    public Collection<Account> listContacts(){        return accountService.getAllAccount();    }   /* @RequestMapping(value = "/accounts/new", method = RequestMethod.GET)    public String createUser() {        System.out.println(" /accounts/new GET ");        return "{\"success\":1}";    }*/    @RequestMapping(value = "/accounts/new", method = RequestMethod.POST)    public ResponseEntity<Void> createUser(@RequestBody Account account, UriComponentsBuilder ucBuilder) {        System.out.println("new account " + account.getName());      /*  if (userService.isUserExist(user)) {            System.out.println("A User with name " + user.getUsername() + " already exist");            return new ResponseEntity<Void>(HttpStatus.CONFLICT);        }*/        accountService.addAccount(account);        HttpHeaders headers = new HttpHeaders();        //headers.setLocation(ucBuilder.path("/user/{id}")).toUri());        return new ResponseEntity<Void>(headers, HttpStatus.CREATED);    }   /* @ExceptionHandler(Exception.class)    public ResponseEntity<String> errorHandler(Exception exc) {        //LOGGER.error(exc.getMessage(), exc);        return new ResponseEntity<>(exc.getMessage(), HttpStatus.BAD_REQUEST);    }*/}